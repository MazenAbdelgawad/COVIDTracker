<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iti.intake40.covidtracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplachActivity"
            android:screenOrientation="locked"
            android:theme="@style/NoThem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activites.NotificationActivity"
            android:theme="@style/NoThem" />
        <activity
            android:name=".view.activites.SelectCountryActivity"
            android:theme="@style/NoThem" />
        <activity
            android:name=".view.activites.SubscribeActivity"
            android:theme="@style/NoThem" />
        <activity
            android:name=".view.activites.HomeActivity"
            android:theme="@style/NoThem"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>